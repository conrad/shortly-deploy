

// mongoDB mongoose node

// [$brew uninstall mongodb]
$ brew install mongodb

$ npm install mongoose --save

$ mkdir /path/to/your/db/dir

$ mongod --dbpath /path/to/your/db/dir

// then in another terminal window:
$ mongo


Schema
model

pre('save')



